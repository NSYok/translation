# Auto detect text files and perform LF normalization
* text=auto

# Explicitly declare text files to ensure consistent line endings
*.py text diff=python
*.json text
*.md text
*.txt text
*.toml text

# Denote all binary files (images) so Git doesn't try to fix line endings
*.png binary
*.jpg binary
*.ico binary
